import 'package:flutter/material.dart';

class LineScreen extends StatelessWidget {
  const LineScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child:Placeholder());
  }
}
